# Icons

Optional icon files for entries, referenced with a relative URL:

```json
"icon": { "url": "./icons/<plugin-id>.svg" }
```

Allowed formats: `.svg`, `.png`, `.webp`, each 256 KB or less. Entries can
use a BB host icon name instead. Publication uploads this directory to
`icons/` under the marketplace URL.
